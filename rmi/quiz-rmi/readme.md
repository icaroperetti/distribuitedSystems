Instalação de pacotes

```
  pip install -r requirements.txt
```

Antes de iniciar o servidor:

```
 pyro4-ns
```

Iniciar o servidor:

```
  python server.py
```

Iniciar o cliente:

```
  python client.py
```
